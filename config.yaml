server:
  host: "127.0.0.1"
  port: 8765

llm:
  model: "gpt-4o-mini"
  temperature: 0.3
  max_tokens: 6000

paths:
  applications_dir: "/mnt/c/Users/LENOVO/desktop/Customy output job applications"
  db_path: "data/customy.db"
  prompts_dir: "prompts"

generation:
  min_score_for_pdf: 0      # skip LaTeX + LLM below this score (0 = disabled)
  min_score_for_queue: 0    # minimum score to appear in Apply Queue (0 = show all)
