# /review-output

Audit the last generated application folder.

Check:

- Does `resume.tex` exist?
- Did the PDF compile?
- Is the SQLite record present for this slug?
- Does the resume stay on one page?
- Are all skills and metrics traceable to `candidate.yaml`?
- Does the language match the JD language?

Report any gap found.
